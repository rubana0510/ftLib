<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.feedbacktower">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-feature android:name="android.hardware.location.gps" />

    <application
            android:name=".App"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            android:usesCleartextTraffic="true"
            tools:ignore="GoogleAppIndexingWarning"
            tools:replace="android:allowBackup,android:usesCleartextTraffic">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
                android:name="com.google.android.geo.API_KEY"
                android:value="@string/google_maps_key"/>

        <activity
                android:name=".ui.map.MapScreen"
                android:label="@string/title_activity_map_screen">
        </activity>
        <activity
                android:name=".ui.videoplayer.VideoPlayerScreen"
                android:theme="@style/AppTheme.NoActionBar">
        </activity>

        <provider
                android:name="androidx.core.content.FileProvider"
                android:authorities="com.feedbacktower.utilities.filepicker"
                android:exported="false"
                android:grantUriPermissions="true">
            <meta-data
                    android:name="android.support.FILE_PROVIDER_PATHS"
                    android:resource="@xml/file_paths"/>
        </provider>

        <activity android:name=".ui.VideoTrimmerScreen">
        </activity>
        <activity android:name=".ui.myplan.MyPlanScreen">
        </activity>
        <activity android:name=".utilities.cropper.ImagePreviewActivity">
        </activity>
        <activity android:name=".ui.qrtransfer.SenderActivity">
        </activity>
        <activity android:name=".ui.ScannerScreen">
        </activity>
        <activity
                android:name=".ui.PostTextScreen"
                android:label="@string/write_something">
        </activity>
        <activity
                android:name=".ui.plans.SubscriptionPlansScreen"
                android:label="@string/select_plan">
        </activity>
        <activity
                android:name=".ui.CustomerMainActivity"
                android:theme="@style/AppTheme.NoActionBar"
                android:windowSoftInputMode="adjustPan">
        </activity>
        <activity
                android:name=".ui.BusinessDetailsActivity"
                android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
                android:name=".BusinessMainActivity"
                android:label="@string/title_activity_business_main"
                android:theme="@style/AppTheme.NoActionBar"
                android:windowSoftInputMode="adjustPan">
        </activity>
        <activity
                android:name=".ui.SplashScreen"
                android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
                android:name=".ui.RegisterPhoneScreen"
                android:theme="@style/AuthTheme">
        </activity>
        <activity
                android:name=".ui.LoginScreen"
                android:theme="@style/AuthTheme">
        </activity>
        <activity
                android:name=".ui.ProfileSetupScreen"
                android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
                android:name=".MainActivity"
                android:label="@string/app_name"
                android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
                android:name=".ui.BusinessProfileSetupScreen"
                android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".utilities.cropper.MyCropperActivity"/>
        <activity android:name=".utilities.cropper.CropImageActivity"/>
        <activity android:name=".ui.qrtransfer.ReceiverActivity"/>
        <activity android:name=".utilities.filepicker.MediaDetailsActivity"/>
        <activity android:name=".utilities.filepicker.FilePickerActivity"/>
        <activity android:name=".ui.BusinessProfileEditScreen"/>

        <receiver
                android:name=".network.utils.ConnectivityReceiver"
                android:enabled="true">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE"/>
            </intent-filter>
        </receiver>

        <service android:name=".notifications.MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service android:name=".utilities.tracker.TrackerService"/>

        <meta-data
                android:name="preloaded_fonts"
                android:resource="@array/preloaded_fonts"/>
    </application>

</manifest>